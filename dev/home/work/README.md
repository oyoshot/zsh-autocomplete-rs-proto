# Disposable completion project

Use this directory to try file creation, Git operations and completion.
Its files and Git history are discarded when the container exits.
The live zacrs checkout is available separately at `/src`, read-only.
